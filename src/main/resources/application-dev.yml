spring.application.name: hello-security

spring:
  datasource:
    url: "jdbc:h2:mem:admindb;DB_CLOSE_DELAY=-1"
    driver-class-name: "org.h2.Driver"
    username: "sa"
    password: ""
  h2:
    console:
      enabled: true
      path: /h2-console # ? ????? ??? ?? ??
  jpa:
    database-platform: "org.hibernate.dialect.H2Dialect"
    hibernate:
      ddl-auto: "create-drop" # ??????? ??? ???? ???? ???
    properties:
      hibernate:
        format_sql: true
        show_sql: true

jwt:
  # ?? ? (?? ?? ????? ?? ?? ??? ???? ???? ?!)
  secret: "vibeCodingWithMinhwanSecretKeyPropertiesAdminSystemAuthJwtToken2026"
  expiration-ms: 86400000 # ?? ?? ?? (???? 24??)